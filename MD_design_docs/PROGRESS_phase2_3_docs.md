# Progress: Phase 2 & 3 Implementation Design Docs

Tracks work on `MD_design_docs/10_phase2_implementation_design.md` and
`MD_design_docs/11_phase3_implementation_design.md`.

- [x] Read roadmap doc 08 (Phases 2 & 3 scope) and template doc 09 (structure/bar to match) — 2026-07-23 16:47 UTC
- [x] Read research-plan docs 07 (splitting strategy) and 05 (model selection) in full — 2026-07-23 16:47 UTC
- [x] Read existing `src/clamp/` layout, `data/README.md`, `schema.py`, `pipeline.py`, `cli.py`, `config.py`, `pyproject.toml` to ground designs in real code, not fiction — 2026-07-23 16:50 UTC
- [x] Create branch `phase2-3-design-docs` from master — 2026-07-23 16:50 UTC
- [x] Draft doc 10 outline (Phase 2: embedding, clustering, splitting) — 2026-07-23 16:55 UTC
- [x] Write doc 10 in full (`MD_design_docs/10_phase2_implementation_design.md`) — 2026-07-23 17:10 UTC — resolved two ambiguities explicitly: doc07 §3.3 vs §3.4 k-range tension (one joint k, HC50-labeled-size floor), and doc08 vs doc07 §3.2 framing of homology partition as cross-check vs. final split (embedding-cluster LOCO is default, homology partition is escalation fallback)
- [x] Draft doc 11 outline (Phase 3: model selection pilot) — 2026-07-23 17:20 UTC
- [x] Write doc 11 in full (`MD_design_docs/11_phase3_implementation_design.md`) — 2026-07-23 17:40 UTC — named package `clamp.model_selection` (not `modeling`/`train`) deliberately to avoid pre-empting Phase 4's namespace; reuses `clamp.splitting.embed`'s loader/mean_pool rather than duplicating
- [x] Self-review both docs against doc 09's bar (concreteness, options+recommendation, DoD, runbook, open questions) — 2026-07-23 17:50 UTC — verified cross-doc consistency (doc11 reuses doc10's `load_encoder`/`mean_pool` signatures exactly), verified all doc05/doc07 section citations against the source text, confirmed no files under src/ or tests/ were touched (docs-only, `git status` shows only the two new MD files + this one)
- [x] Commit — 2026-07-23 17:52 UTC
- [x] Push branch — 2026-07-23 17:53 UTC
- [x] Open PR to Eitzl/CLAMP master — 2026-07-23 17:56 UTC — https://github.com/Eitzl/CLAMP/pull/5
- [x] Rename `PROGRESS.md` -> `MD_design_docs/PROGRESS_phase2_3_docs.md` — 2026-07-23 18:12 UTC — fixed a merge conflict: PR #4 (a different branch) had already merged its own root-level `PROGRESS.md` into master, colliding with this branch's independently-added file of the same name
- [x] Address CodeRabbit's 17 actionable comments (2026-07-23, orchestrator-applied directly on this branch): fixed 14, deferred 2 to the project owner, rejected 1 as a false positive:
  - Doc 10 fixed: `FoldAssignment.cluster_id` renamed to `partition_id`+`partition_method` (was silently conflating k-means cluster IDs with homology community IDs); config field/model validators added (positive sizes, PCA variance/homology-identity ranges, `kmeans_k_min<=k_max`); `select_k` given a concrete deterministic ranking formula; `partition_leiden_fallback` now recomputes leakage diagnostics post-reassignment; `build_loco_folds` given a fully deterministic rotate/fixed_smallest selection rule; Phase 2 given its own `manifest_phase2.json` namespace + config-aware cache keys; `cluster`/`validate` stage input/output declarations completed (missing `agreement_report.json` output, missing dataset/embeddings inputs).
  - Doc 10 rejected: CodeRabbit's suggested fix for `threshold_sensitivity_probe` had the threshold direction backwards relative to doc 07's own explicit definition ("exceeds threshold removed" + "as you allow more train/test homology, apparent performance goes up") — verified against doc 07 lines 88/97/194 before touching anything; kept the original claim, only tightened the docstring wording so it can't be misread the same way again.
  - Doc 10 deferred (needs the project owner, not a mechanical fix): §10/§14 DoD validation gates have no actual numeric pass/fail thresholds for NN-distance/label-balance/composition/ARI — needs real domain judgment, not invented numbers.
  - Doc 11 fixed: `train_baseline` now imputes on train-only before `ElasticNetCV`; candidate registry redesigned (`_CANDIDATE_TEMPLATES` + `resolve_registry()`) so the revision-pin test is normal/always-green instead of permanently-skipped/red; v1 escalation given one authoritative gate (`--include-v1`, settings value is just its default); `compute_metrics` returns `None` for non-finite correlations instead of leaking NaN into ranking; `DecisionResult`/`rank_candidates` redesigned around an explicit `CandidateStatus`/`CandidateOutcome` so skipped/failed/incomplete/succeeded are distinguishable; `probe()` CLI actually wired for its documented `--full` option; default-grid-size wording fixed (4 v2 executed by default, 5 registered).
  - Doc 11 deferred (needs the project owner — real methodology/p-hacking-risk call, not mechanical): multi-seed aggregation policy for `PilotResult` (mean-across-seeds vs. best-seed-selection) left unresolved in the doc.
- [x] Commit and push follow-up fixes — 2026-07-23 18:35 UTC
- [x] Resolved the doc 10 §10/§17 DoD deferred item per project owner's guidance — 2026-07-23 18:55 UTC. Owner specified `mean + k*std` as the threshold form and that k should be set low (more sensitive) given this is a precision-critical use case. Implemented as a new §10.0 "Statistical threshold framework": k=2 for every gate (vs. the more permissive k=3), with each of the five §10 checks (plus the ARI agreement diagnostic) given its own concrete reference distribution and direction — upper-tail for max-identity (reusing the existing `homology_identity_threshold` as the boundary rather than a new number), lower-tail for embedding NN distance (background = train-to-train NN distances), a significance-of-trend form for threshold-sensitivity (Fisher-z SE), two-sided for label balance and composition (overall mean/std, and a binomial-proportion SE respectively), and a permutation-null form for the ARI diagnostic (no natural std of its own). Updated `report.py`'s contents list and the Definition of Done (§17) to require the actual computed mean/std/boundary be shown, not just a verdict. Also fixed two stale `§14` cross-references that should have been `§17` (Definition of Done) while in the area.
- [x] Resolved the doc 11 multi-seed aggregation deferred item per project owner's confirmation ("mean across seeds is standard and optimal") — 2026-07-23 19:25 UTC. Added §9.3 with `aggregate_finetune_results`: mean across seeds for mae/spearman_rho/pearson_r (explicitly NOT best-seed-by-test-performance, the p-hacking pattern this item existed to avoid), SUM (not mean) for `wall_clock_s` since it's a compute-cost figure, and a conservative rule that any single seed's `None` correlation (§10's non-finite-metric policy) poisons the whole aggregate rather than silently averaging over the remaining finite seeds. Added `PilotResult.n_seeds` so the aggregation is auditable from the row itself. Updated `run_grid`'s docstring, the storage-layout comment for `pilot_results.parquet`, and added a testing-table row covering the aggregation function directly (including the "one degenerate seed poisons the aggregate" regression case).
